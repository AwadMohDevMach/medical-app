import React from 'react'

export default function Layout({children}:{children : React.ReactNode}) {
  return (
    <div>
      <h2>i am daxhboard only pages layout</h2>
      {children}
    </div>
  )
}
